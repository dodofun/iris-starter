syntax = "proto3";

package protos;

option go_package = "./protos";

message UserRequest {
    string name = 1;
}

message UserResponse {
    string message = 1; 
}

service Greeter {
  // Sends a greeting
  rpc SayHello (UserRequest) returns (UserResponse) {}
}